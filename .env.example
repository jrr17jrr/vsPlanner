# VSLead — variáveis de ambiente
#
# Nesta primeira versão (front-end com dados mock + localStorage) nenhuma
# variável é obrigatória para rodar o projeto. As chaves abaixo já estão
# preparadas para quando o Supabase for conectado.

NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
